// import $ from 'jquery';
// import { chromeSyncStorage } from 'utils/storage';
import './style.scss';

const blockPosts = () => {
    // const $postLis = $('.nk-content .common-list li');
    // $postLis.each(function() {
    //     const links = $(this).find('.discuss-main a');
    //     if (links.length >= 1) {
    //         shouldShadow && $(this).remove();
    //     }
    // });
    // const blockConfig = chromeSyncStorage.get(items => {
    //     console.log(items);
    // });
};

blockPosts();
